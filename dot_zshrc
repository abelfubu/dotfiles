eval "$(zoxide init zsh)"
eval "$(starship init zsh)"
eval "$(mise activate zsh)"
if command -v wt >/dev/null 2>&1; then eval "$(command wt config shell init zsh)"; fi
eval "$(atuin init zsh)"

### MANAGED BY RANCHER DESKTOP START (DO NOT EDIT)
export PATH="/Users/abeldelafuente/.rd/bin:$PATH"
### MANAGED BY RANCHER DESKTOP END (DO NOT EDIT)

export PATH="$HOME/.local/share/bin/:$PATH"
export ZPLUG_HOME=/opt/homebrew/opt/zplug
source $ZPLUG_HOME/init.zsh

export BAT_THEME=ansi
export FZF_DEFAULT_OPTS="\
--preview='bat --color=always {}' \
--bind ctrl-u:preview-up,ctrl-d:preview-down,ctrl-p:toggle-preview \
--multi"

export EDITOR=nvim

setopt NO_BEEP
setopt APPEND_HISTORY
HISTFILE=$HOME/.zhistory
SAVEHIST=1000
HISTSIZE=999
setopt EXTENDED_HISTORY

# zsh-users
# zplug "zsh-users/zsh-completions"
zplug "zsh-users/zsh-autosuggestions"
zplug "zsh-users/zsh-syntax-highlighting", defer:2


if ! zplug check; then
    zplug install
fi

zplug load


autoload -U compinit && compinit
export CARAPACE_BRIDGES='zsh,fish,bash,inshellisense' # optional
zstyle ':completion:*' format $'\e[2;37mCompleting %d\e[m'
source <(carapace _carapace)

function y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	command yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ "$cwd" != "$PWD" ] && [ -d "$cwd" ] && builtin cd -- "$cwd"
	command rm -f -- "$tmp"
}

alias vi='nvim'
alias vim='nvim'
alias la='eza -la --icons'
alias ll='eza -l --icons'
alias ls='eza --icons'
alias lt='eza --icons -TL'
alias gs='git status'
alias gsc='git switch -c'
alias gb='git switch -'
alias gsw='git switch'
alias fzed='zed $(fzf)'

alias gg='lazygit'
alias dd='lazydocker'
alias ss='lazysql'

# bun completions
[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"
# Load machine-specific secrets (not in chezmoi)
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
export PATH="$HOME/.local/bin:$PATH"
export PATH="/opt/homebrew/opt/libpq/bin:$PATH"

# Lazygit config location
export LG_CONFIG_FILE="$HOME/.config/lazygit/config.yml"

# kimi-code
export PATH="$HOME/.kimi-code/bin:$PATH"
export PATH="$HOME/dev/git-architect/bin:$PATH"


if [[ $HERDR_ENV == 1 ]]; then
  nvim() { TERM=xterm-ghostty command nvim "$@"; }
fi
